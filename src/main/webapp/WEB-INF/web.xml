<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee
                             https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
         version="6.0">

    <display-name>TeamWork Hub</display-name>

    <!-- Cấu hình thời gian Session sống trong 30 phút -->
    <session-config>
        <session-timeout>30</session-timeout>
    </session-config>

    <!-- CẤU HÌNH FILTER BẢO MẬT TẬP TRUNG -->
    <filter>
        <filter-name>AuthFilter</filter-name>
        <filter-class>com.teamwork.filters.AuthFilter</filter-class>
    </filter>
    <filter-mapping>
        <filter-name>AuthFilter</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>

    <!-- 1. AuthServlet Mapping (/auth) -->
    <servlet>
        <servlet-name>AuthServlet</servlet-name>
        <servlet-class>com.teamwork.controllers.AuthServlet</servlet-class>
    </servlet>
    <servlet-mapping>
        <servlet-name>AuthServlet</servlet-name>
        <url-pattern>/auth</url-pattern>
    </servlet-mapping>

    <!-- 2. ProjectServlet Mapping (/project) -->
    <servlet>
        <servlet-name>ProjectServlet</servlet-name>
        <servlet-class>com.teamwork.controllers.ProjectServlet</servlet-class>
    </servlet>
    <servlet-mapping>
        <servlet-name>ProjectServlet</servlet-name>
        <url-pattern>/project</url-pattern>
    </servlet-mapping>

    <!-- 3. TaskServlet Mapping (/task) -->
    <servlet>
        <servlet-name>TaskServlet</servlet-name>
        <servlet-class>com.teamwork.controllers.TaskServlet</servlet-class>
    </servlet>
    <servlet-mapping>
        <servlet-name>TaskServlet</servlet-name>
        <url-pattern>/task</url-pattern>
    </servlet-mapping>

    <!-- 4. DocServlet Mapping (/doc) -->
    <servlet>
        <servlet-name>DocServlet</servlet-name>
        <servlet-class>com.teamwork.controllers.DocServlet</servlet-class>
    </servlet>
    <servlet-mapping>
        <servlet-name>DocServlet</servlet-name>
        <url-pattern>/doc</url-pattern>
    </servlet-mapping>

    <!-- 5. ChatServlet Mapping (/chat) -->
    <servlet>
        <servlet-name>ChatServlet</servlet-name>
        <servlet-class>com.teamwork.controllers.ChatServlet</servlet-class>
    </servlet>
    <servlet-mapping>
        <servlet-name>ChatServlet</servlet-name>
        <url-pattern>/chat</url-pattern>
    </servlet-mapping>

    <!-- Trang mặc định khi truy cập root URL -->
    <welcome-file-list>
        <welcome-file>index.jsp</welcome-file>
    </welcome-file-list>

</web-app>
